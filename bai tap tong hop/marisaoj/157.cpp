#include <bits/stdc++.h>
using namespace std;
#define int long long
#define fi first
#define se second
#define oo 1e18
const int MM = 507;
int n,a[MM],dp[MM][MM];



signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> n;
    for(int i = 1;i <= n;i++) dp[i][i] = a[i];
    for(int i = 1;i <= n;i++) {
        for(int len = 2;len <= n;len++) {
            for(int j = i+len-1;j <= )
        }
    }
    return 0;
}