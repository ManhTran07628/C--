#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define fi first
#define se second
#define oo 1e18
#define BIT(x,y) ( ((x) >> (y)) & 1)
const int MM = 1e6+7;
ll x,y,a,b,B;
ll fib[MM];


void solve()
{
    cin >> x >> y >> a >> b >> B;

}

signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    fib[1] = fib[2] = 1;
    for(int i = 3;i <= 500;i++) {
        fib[i] = fib[i-1] % 
    }
    return 0;
}