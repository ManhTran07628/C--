#include <bits/stdc++.h>
using namespace std;
#define int long long
#define fi first
#define se second
const int MAXN = 1e6+7;
int dp[501][501],dps[501][501] 
// dp[i][k] so thao tac it nhat de tao thanh xau doi xung thu k 
// den vi tri i
// dps[i][j] so thao tac it nhat de tao ra xau doi xong tai vi tri i
// va vi tri j cua xau s

signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int k;
    string s;
    cin >> k >> s;
    int n = s.size();
    s = ' ' + s;
    for(int i = 1;i <= n;i++) {
        
    }
    return 0;
}