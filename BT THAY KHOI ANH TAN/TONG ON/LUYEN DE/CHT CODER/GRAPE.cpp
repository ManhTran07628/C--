#include <bits/stdc++.h>
using namespace std;
#define int long long
#define fi first
#define se second
const int MAXN = 1e6+7;
double a[MAXN],d[MAXN];
signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    for(int i = 1;i <= n;i++) {
        cin >> a[i];
    }
    double ans = 0;
    for(int i = 1;i <= n;i++) {
        for(int j = i;j <= n;j++) {
            int l = 
        }
    }
    return 0;
}