#include <bits/stdc++.h>
using namespace std;
#define ll long long
const int N = 1e7+1;
ll a[N];
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    #define taskname "SphenicNum"
    if(fopen(taskname ".inp","r"))
    {
        freopen(taskname ".inp","r",stdin);
        freopen(taskname ".out","w",stdout);
    }
    ll t;
    cin >> t;
    while(t--)
    {
        // ll n;
        // cin >> n;
        // ll dem = 0,kt = 1;
        // for(int i = 2;i <= n;i++) {
        //     ll stt = 0;
        //     while(n % i == 0) {
        //         n /= i;
        //         stt++;
        //     }
        //     if(stt > 1) {
        //         kt = -1;
        //         break;
        //     }
        //     else if(stt == 1) dem++;
        // }
        // if(kt == -1 || dem != 3) cout << 0 << '\n';
        // else cout << 1 << '\n';
        ll n;
        cin >> n;
        for(int i = 1;i <=)
    }
    return 0;
}
